[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "==2.3.3"
pandas = ">=1.5.0"
numpy = ">=1.21.0"
scikit-learn = ">=1.0.0"
matplotlib = ">=3.5.0"
seaborn = ">=0.11.0"
plotly = ">=5.0.0"
joblib = ">=1.1.0"
werkzeug = ">=2.0.0"
jinja2 = ">=3.0.0"
requests = ">=2.25.0"

[dev-packages]

[requires]
python_version = "3.13"
